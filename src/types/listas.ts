export type lista_menu = {
    titulo: string, link: string, 
}

export type lista_footer = {
    lista: string,
}

export type lista_home = {
    id: number, titulo_pequeno: string, titulo: string, paragrafo: string,
    texto_botao: string, texto_mini: string
}

export type lista_informacoes = {
    id: number, imagem: string, titulo: string, paragrafo: string,
}

export type lista_card = {
    id_card: number, titulo_card: string, paragrafo_card: string, paragrafo_conta_card: string, texto_pequeno_card: string,
}

export type exemplo_lista_card = {
    listagem: string,
}